// Animation for loader
const loader = document.querySelector('.loader');
const dots = loader.querySelectorAll('div');

// Optional: Add any additional JavaScript functionality here
console.log('Under Maintenance Page Loaded');

// Ensure the logo is loaded properly
const logo = document.querySelector('.logo img');
if (logo) {
    console.log('Logo loaded successfully');
}

// Ensure the maintenance image is loaded properly
const maintenanceImage = document.querySelector('.maintenance-image img');
if (maintenanceImage) {
    console.log('Maintenance image loaded successfully');
}

// Enhance loader animation
console.log('Funny loader animation active');
